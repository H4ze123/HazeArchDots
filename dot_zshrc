##
## ZSHRC
## Created by : @GeodeArc
##

# EXPORTS (you will wanna add stuff here later)
export EDITOR=nvim

# BASH/ZSH HISTORY
# i decided to combine with bash history, but you can change this if you like
HISTFILE=~/.bash_history
HISTSIZE=10000
SAVEHIST=10000

setopt appendhistory
setopt autocd
setopt nomatch
setopt correct

# ALIASES (see separate file)
if [ -f "$HOME/.config/sh/aliases.sh" ]; then
    . "$HOME/.config/sh/aliases.sh"
fi

# PROMPT
eval "$(starship init zsh)"
if [[ $(tty) == *"pts"* ]]; then
   fastfetch --logo-type small --logo-padding 3 --logo-padding 4
else
    echo
    if [ -f /bin/hyprctl ]; then
        echo "Start Hyprland with command start-hyprland"
    fi
fi

export PATH="$PATH:/home/haze/.local/bin"

#export PYENV_ROOT="$HOME/.pyenv"
#export PATH="$PYENV_ROOT/bin:$PATH"
#eval "$(pyenv init -)"
